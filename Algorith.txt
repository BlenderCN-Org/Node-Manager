function getDirection(loop):
  if y.coord of verts in loop is not monotonous:
   return y
  else if x.coord of verts in loop is not monotonous:
   return x
  else if dx < dy:
   return x
  else if dy < dx:
   return y
   
   
for edges in selected:
  loop = edges.select_loop
  scale(direction = getDirection(loop))
